# summer
A simple file hash checking executable that does the finicky hash comparing for you.
## Usage
`summer [algorithm] [file] [expected hash]`
Where `algorithm` is sha256 or md5, `file` is a valid file path, and `expected hash` is the hash that you expect from the file.
